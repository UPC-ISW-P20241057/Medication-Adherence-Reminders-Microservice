server:
  port: 8080
logging:
  level:
    org:
      hibernate: debug
      springframework: debug

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://azure-mau-postgre.postgres.database.azure.com:5432/medication_database
    username: mau
    password: Li@nel2022
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  swagger-ui:
    path: /swagger-ui.html
